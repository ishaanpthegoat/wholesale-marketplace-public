/**
 * PLACEHOLDER — generated types go here.
 *
 * Run `npm run db:types` after starting the local Supabase stack. That command
 * overwrites this file with the real schema types. Re-run it after every
 * migration.
 */
export type Database = Record<string, unknown>;

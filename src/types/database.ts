/**
 * PLACEHOLDER — generated types go here.
 *
 * Run `npm run db:types` after starting the local Supabase stack. That command
 * overwrites this file with the real schema. Re-run it after every migration.
 *
 * The shape below is the minimum `@supabase/ssr` needs to infer its generics;
 * an empty object breaks the client's parameter types.
 */
export type Database = {
  public: {
    Tables: Record<string, never>;
    Views: Record<string, never>;
    Functions: Record<string, never>;
    Enums: Record<string, never>;
    CompositeTypes: Record<string, never>;
  };
};
